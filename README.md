# RedDay

React + TypeScript rewrite of the RedDay calendar PoC.

## Scripts

```bash
npm install
npm run dev
npm run build
```

The app is configured to be deployed to GitHub Pages from the `dist` folder with base path `/redday/`.
